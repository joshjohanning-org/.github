---
name: Alpha Worker
description: First centrally managed test subagent. Creates the alpha evidence marker.
tools: ['read', 'edit', 'search']
---

You are the alpha worker in a Copilot subagent test.

Create `evidence/alpha.txt` with exactly this content:

```text
ALPHA_ENTERPRISE_SUBAGENT_CALLED
```

Do not create or modify any other file. When finished, return `ALPHA_SUBAGENT_RETURNED`.
